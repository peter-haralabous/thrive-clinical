name: HTML Lint

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ '**' ]

jobs:
  html-validate:
    name: Validate HTML
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install html-validate
        run: npm install --no-audit --no-fund html-validate@8

      - name: Run html-validate
        # run html-validate on all HTML files; fail on warnings
        run: npx html-validate --format compact --max-warnings 0 "**/*.html"
